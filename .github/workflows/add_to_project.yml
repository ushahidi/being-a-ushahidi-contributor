name: 📜 Add article issues 

on:
  issues:
    types:
      - opened

jobs:
  add-to-project:
    name: Add issue to project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v1.0.2
        with:
          project-url: https://github.com/orgs/ushahidi/projects/26
          github-token: ${{ secrets.THE_OCTO_SECRET }}
          labeled: article, submission
          label-operator: AND
